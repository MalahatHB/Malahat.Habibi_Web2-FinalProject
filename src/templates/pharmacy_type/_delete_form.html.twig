<form method="post" action="{{ path('app_pharmacy_type_delete', {'id': pharmacy_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pharmacy_type.id) }}">
    <button>Delete</button>
</form>
