{% extends 'base.html.twig' %}

{% block title %}PharmacyType{% endblock %}

{% block body %}
    <h1>PharmacyType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pharmacy_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pharmacy_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pharmacy_type_index') }}">back to list</a>

    <a href="{{ path('app_pharmacy_type_edit', {'id': pharmacy_type.id}) }}">edit</a>

    {{ include('pharmacy_type/_delete_form.html.twig') }}
{% endblock %}
