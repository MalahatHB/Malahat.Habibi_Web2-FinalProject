{% extends 'base.html.twig' %}

{% block title %}DrugWarehouse{% endblock %}

{% block body %}
    <h1>DrugWarehouse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ drug_warehouse.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ drug_warehouse.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ drug_warehouse.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_drug_warehouse_index') }}">back to list</a>

    <a href="{{ path('app_drug_warehouse_edit', {'id': drug_warehouse.id}) }}">edit</a>

    {{ include('drug_warehouse/_delete_form.html.twig') }}
{% endblock %}
