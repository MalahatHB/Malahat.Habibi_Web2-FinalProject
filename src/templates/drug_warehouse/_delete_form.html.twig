<form method="post" action="{{ path('app_drug_warehouse_delete', {'id': drug_warehouse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ drug_warehouse.id) }}">
    <button>Delete</button>
</form>
