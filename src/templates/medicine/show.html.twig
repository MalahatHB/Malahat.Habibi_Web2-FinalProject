{% extends 'base.html.twig' %}

{% block title %}Medicine{% endblock %}

{% block body %}
    <h1>Medicine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicine.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ medicine.name }}</td>
            </tr>
            <tr>
                <th>ShortDescription</th>
                <td>{{ medicine.shortDescription }}</td>
            </tr>
            <tr>
                <th>FullDescription</th>
                <td>{{ medicine.fullDescription }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ medicine.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medicine_index') }}">back to list</a>

    <a href="{{ path('app_medicine_edit', {'id': medicine.id}) }}">edit</a>

    {{ include('medicine/_delete_form.html.twig') }}
{% endblock %}
