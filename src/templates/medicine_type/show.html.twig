{% extends 'base.html.twig' %}

{% block title %}MedicineType{% endblock %}

{% block body %}
    <h1>MedicineType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicine_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ medicine_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medicine_type_index') }}">back to list</a>

    <a href="{{ path('app_medicine_type_edit', {'id': medicine_type.id}) }}">edit</a>

    {{ include('medicine_type/_delete_form.html.twig') }}
{% endblock %}
