<form method="post" action="{{ path('app_medicine_type_delete', {'id': medicine_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medicine_type.id) }}">
    <button>Delete</button>
</form>
